# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=apache
REPOSITORY?=httpd
TAG?=2.4.46-alpine

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)


default: image

image:
	docker build --build-arg=https://10.130.0.20:7890 \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)
