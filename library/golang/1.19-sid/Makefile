# This file is generated by the template.

REGISTRY     ?= lcr.loongnix.cn
ORGANIZATION ?= library
REPOSITORY   ?= golang
TAG          ?= 1.19-sid

IMAGE = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
ALIAS = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):1.19

default: image

image:
	echo "10 09 test"
	docker build \
		--build-arg https_proxy=$(https_proxy) \
		-t $(IMAGE) \
		-t $(ALIAS) \
		.

push:
	docker push $(IMAGE)
	docker push $(ALIAS)

