# This file is generated by the template.

REGISTRY     ?= lcr.loongnix.cn
ORGANIZATION ?= prometheus
REPOSITORY   ?= golang
TAG          ?= 1.21-buster-prometheus

IMAGE  = $(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST =

default: image

image:
	docker build \
                -t $(IMAGE) \
                .

push:
	docker push $(IMAGE)

ifneq ($(LATEST),)
	docker tag $(IMAGE) $(LATEST)
	docker push $(LATEST)
endif
