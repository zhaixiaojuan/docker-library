# This file is generated by the template.

REGISTRY	?=cr.loongnix.cn
ORGANIZATION	?=tonistiigi
REPOSITORY	?=bats-assert
TAG		?=1.3.0
LATEST		?=true

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
GIT_URL=https://github.com/Loongson-Cloud-Community/xx.git

default: image

src:
	git clone -b loong64-1.3.0 $(GIT_URL) $@

image: src
	cd src/util/bats-assert &&  docker buildx bake -f docker-bake.hcl

push:
	cd src/util/bats-assert && docker buildx bake --push -f docker-bake.hcl

clean:
	rm -rf src
