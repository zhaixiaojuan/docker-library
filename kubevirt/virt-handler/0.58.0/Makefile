# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=kubevirt
REPOSITORY?=virt-handler
TAG?=0.58.0
LATEST?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest

default: image

download:
	no_proxy=cloud.loongnix.cn wget -O container-disk http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/container-disk
	no_proxy=cloud.loongnix.cn wget -O virt-chroot http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-chroot-v0.58.0-linux-loong64
	no_proxy=cloud.loongnix.cn wget -O virt-handler http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-handler-v0.58.0-linux-loong64
	chmod +x virt-chroot virt-handler container-disk	

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
