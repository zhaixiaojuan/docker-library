# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=kubevirt
REPOSITORY?=virt-api
TAG?=0.58.0
LATEST?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest
DOWNLOAD_URL=http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-api-v0.58.0-linux-loong64
default: image

download:
	no_proxy=cloud.loongnix.cn wget -O virt-api $(DOWNLOAD_URL)
	chmod +x virt-api

image: download
	docker build \
		-t $(IMAGE) \
		.

push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
