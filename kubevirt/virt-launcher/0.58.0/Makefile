# This file is generated by the template.

REGISTRY?=lcr.loongnix.cn
ORGANIZATION?=kubevirt
REPOSITORY?=virt-launcher
TAG?=0.58.0
LATEST?=false

IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):$(TAG)
LATEST_IMAGE=$(REGISTRY)/$(ORGANIZATION)/$(REPOSITORY):latest

default: image

download:
	no_proxy=cloud.loongnix.cn wget -O container-disk http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/container-disk
	no_proxy=cloud.loongnix.cn wget -O virt-launcher http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-launcher-v0.58.0-linux-loong64
	no_proxy=cloud.loongnix.cn wget -O virt-launcher-monitor http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-launcher-monitor-v0.58.0-linux-loong64
	no_proxy=cloud.loongnix.cn wget -O virt-freezer http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-freezer-v0.58.0-linux-loong64
	no_proxy=cloud.loongnix.cn wget -O virt-probe http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/openeuler-22.03/virt-probe-v0.58.0-linux-loong64
	no_proxy=cloud.loongnix.cn wget http://cloud.loongnix.cn/releases/loongarch64/kubevirt/kubevirt/0.58.0/libvirt-9.10.0-rpm.tar.gz
	chmod +x container-disk virt-launcher virt-launcher-monitor virt-freezer virt-probe
	tar -zxf libvirt-9.10.0-rpm.tar.gz

image: download
	docker build \
		-t $(IMAGE) \
		.

clean:
	rm -rf container-disk \
		virt-launcher \
		virt-launcher-monitor \
		virt-freezer \
		virt-probe \
		libvirt-9.10.0-rpm.tar.gz \
		libvirt-9.10.0-rpm

push:
	docker push $(IMAGE)
	#latest image
	@if [ $(LATEST) = "true" ]; \
		then \
		docker tag $(IMAGE) $(LATEST_IMAGE); \
		docker push $(LATEST_IMAGE); \
	fi
